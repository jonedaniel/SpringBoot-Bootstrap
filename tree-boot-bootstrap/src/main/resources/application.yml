server:
  port: 8080
  context-path: ""
mybatis:
  mapper-locations: "classpath:mapper/*Mapper.xml"

spring:
  profiles:
    active: prod
management:
  endpoints:
    web:
      exposure:
        include: "*"
    enabled-by-default: true
  endpoint:
    health:
      show-details: always
  server:
    port: 9999



